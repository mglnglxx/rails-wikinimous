<div class="container">
<ul class='list-unstyled'>
  <% @articles.each do |article| %>
    <h2> <li class="title"> <%= link_to article.title, article_path(article) %></li> </h2>
    <p> <li class="content"> <%= article.content %> </li> </p>
  <% end %>
</ul>
</div>
